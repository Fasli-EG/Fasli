-- ============================================
-- لون واجهة منفصل للمساعد (مستقل عن لون المدرس) — الشعار يفضل نفس شعار المدرس دايمًا
-- ============================================
alter table assistants add column if not exists brand_color text;
